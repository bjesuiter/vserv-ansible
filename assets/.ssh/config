# This config configures the ssh connection settings to the hosts which should be configured via ansible
#
# Note: 
# The most general Settings should go at the bottom of this file 
# because the first setting which can be found will be used. 

Host *.avil.io: 
    IdentityFile: ~/.ssh/ansible@macos4vserv
